# 机密信息保存规则

## 原则
凡是需要长期保存且未来可能取回使用的机密，统一按“密文入库、明文不入库”的方式处理。

## 规则
1. 在文档中只记录非机密接入说明
2. 将机密值加密后保存到 secrets/ 下
3. 在 docs/ops/ 下记录加密方法和解密方式
4. 不得把明文密码、token、key 写入仓库
5. 若需要取回使用，只能通过密文 + 单独掌握的口令恢复

## 当前状态
- 邮箱接入说明可入库
- 邮箱密码属于机密，不得明文入库
- secrets/ 下尚未写入任何明文或密文材料，待具备稳定加密流程后再纳入
